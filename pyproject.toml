[project]
name = "mindle-media-ai"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["Pillow>=10.0", "opencv-python-headless>=5.0"]

[project.optional-dependencies]
dev = ["pytest>=9.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
