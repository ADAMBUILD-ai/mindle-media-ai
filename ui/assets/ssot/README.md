# Approved UI Source Asset

Place the user-approved UI image or source export in this directory. Record its SHA-256, approval date and filename in `../../ssot_manifest.json` before implementing pixel geometry or screenshot regression.

No substitute image may be generated or selected by inference.
