# UI Skeleton

`index.html` fixes the approved two-stack video and photo component order, panel ownership, command controls, reference upload controls, preview and timeline slots. It deliberately contains no CSS geometry, color, or visual redesign because the approved screenshot/source is absent from both the official repository and local package.

Add CSS only from the approved UI source or image reference. Do not replace the component order or remove either command or reference-upload control.
