# MINDLE MEDIA AI PC Local UI File Chooser Replay Directive v33.0

Date: 2026-09-25  
Repository: `ADAMBUILD-ai/mindle-media-ai`  
PR: `#22`  
Branch: `work/v28-1-remote-runtime-package-20260924`

## Starting state

The v32 closeout is PASS for the real local CPU runtime and product backend path. Do not repeat or invalidate the frozen remote package, license, canonical, remote Product E2E, SAM 2.1 PHOTO, Intel SISR 4x, VIDEO tracking, Korean STT, project save, or project export evidence.

## Only remaining task

Re-run the unchanged approved local UI with the restored PHOTO, VIDEO, and Korean audio inputs through the actual browser file chooser and capture independent UI evidence for Preview → Save → Export. Keep `ui/index.html`, `ui/interaction.js`, `ui/product_integration.js`, CSS, and SSOT unchanged.

## Acceptance

1. Use the already materialized exact package and CPU-only Windows runtime.
2. Use the existing actual input hashes recorded in `MINDLE_MEDIA_AI_PC_TORCH_RECOVERY_FINAL_LOCAL_E2E_EVIDENCE_v32_20260925.json`.
3. Verify the browser-visible preview for segmentation, upscale, tracking, and transcript.
4. Click the existing Save and Export controls and record the resulting project/export IDs and SHA-256 values.
5. Do not call process success a quality PASS; preserve the v32 backend evidence.
6. If native file injection is unavailable, stop at a clearly scoped REWORK with the exact automation limitation. Do not change product UI to work around the limitation.

## Output

Write the replay result under `docs/commander/` and `evidence/pc_remote/`, commit to this branch only, and report the exact path and SHA. No main merge, production deployment, force push, GPU, or paid compute.

