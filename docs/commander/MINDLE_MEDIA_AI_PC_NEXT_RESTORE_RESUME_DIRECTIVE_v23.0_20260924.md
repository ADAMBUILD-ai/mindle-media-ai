# MINDLE MEDIA AI — PC NEXT RESTORE/RESUME DIRECTIVE v23.0

Date: 2026-09-24
Status: WAIT_FOR_RESTORE_PATH
Base: v22 verified asset restore review and evidence

## Verdict

The v22 baseline reconciliation is complete. Remote adopted assets and prior approved real-input evidence are preserved. The remaining failure is the PC restore path, not absence of product assets.

## Resume conditions

Provide one approved PC-local restore path:

A. Securely expose the existing scoped HF credential to the PC-local process and permit extraction of the preserved E2E artifact into the project workspace; or

B. Restore the adopted cache through the approved authenticated project path and expose the prior approved video/Korean-audio inputs locally.

Never paste credentials into chat, terminal history, source, logs, PR, or Evidence.

## After restore

1. Verify SAM 2.1, whisper-small, and Intel SISR exact revisions and hashes.
2. Resolve OpenVINO CPU and ffmpeg with a permitted local/offline installer or preloaded package source; record version/path.
3. Run PHOTO and UPSCALE immediately.
4. Reuse the preserved approved video and Korean audio if provenance is intact; otherwise use the local picker for approved replacements.
5. Run VIDEO tracking and Korean STT.
6. Validate Preview, save, export, hashes, dimensions/continuity, transcript, and quality.
7. Run Python/UI/server/evidence/package regressions.
8. Create the next review and next directive with commit/PR/Actions status.

## Protection

UI_SSOT_CHANGED:NO. No UI redesign, main merge, Production deploy, force push, GPU, paid compute, legacy fallback, secret exposure, or overwrite of prior Evidence.
