# MINDLE MEDIA AI — REMOTE PACKAGE SUCCESS → PC QUALITY E2E DIRECTIVE v29.0
Date: 2026-09-24
Status: EXECUTE IMMEDIATELY
Base: PR #22 head 5d871373fa2ba5ceb69e4abc7aca36d504f4f3f3

## Commander verification
Remote packaging breakthrough is VERIFIED PASS.
Run 35974551384 completed SUCCESS.
Product E2E run 35974551391 SUCCESS.
Canonical 35974551506 SUCCESS.
License Gate 35974551394 SUCCESS.

Verified runtime transfer artifacts exist:
- part-00 artifact 10796639261, 419251237 bytes, digest sha256:b930e521bb627f02134984ded6e7eb8ace20e15c2e56d9b9632c4759aaa62224
- part-01 artifact 10797756522, 418706393 bytes, digest sha256:8bc5a6df3759a48b50efce8c5984c65057ec57ea4270935a2c11359f5835b128
- part-02 artifact 10797646925, 78665503 bytes, digest sha256:c2ad3507f48c1b5ee54f1063af868ef85be3d2f19b689efdf3f2df6ec0272b4e

Do not redo model shopping or remote packaging unless a digest/file verification fails.

## 1. Transfer all three artifacts to PC
Use the same proven GitHub artifact Download handoff.
Materialize part-00, part-01, part-02 into one approved project staging folder.
If browser download requires a user click, surface each actual Download control and continue automatically after file selection.
Do not return another generic transfer report.

## 2. Verify package integrity
For each downloaded artifact verify local byte size and GitHub artifact digest before extraction.
On mismatch: re-download only the failed part.
On match: PASS and continue.

## 3. Extract/reassemble verified runtime package
Extract all parts according to the package manifest.
Inventory exact SAM 2.1, whisper-small, Intel SISR files and OpenVINO runtime package.
Verify model revisions/bytes/hashes/license manifest against the frozen adopted identities.
Only exact verified assets enter local cache.

## 4. Install/expose CPU runtime locally
Install/expose packaged OpenVINO CPU from the verified transfer package.
For video, prefer the already-working OpenCV path if it satisfies the existing adapter; do not block solely on ffmpeg.
Record runtime versions and health/load tests.

## 5. PHOTO quality REWORK
Run actual local SAM 2.1 on the recovered approved photo.
The old wrong-subject overlay is rejected and must not be reused.
Before inference verify adapter resize/coordinate/prompt mapping/mask-selection logic.
Fix adapter if necessary.
PASS only with intended-subject usable boundary.
Generate before/after evidence.

## 6. UPSCALE quality REWORK
Run actual local Intel SISR/OpenVINO.
The old black output is rejected and must not be reused.
Verify dtype, normalization, layout, tensor names, output range, channel order, clipping and encoding.
PASS only with true 4x dimensions, non-black content, reopen success and useful visual detail.

## 7. VIDEO local execution
Use the recovered valid 125-frame video.
Run actual local SAM tracking.
Validate continuity, decodability, Preview, project save and export.
OpenCV path is acceptable if it is the supported working path.

## 8. Korean STT local execution
Use the recovered valid 16 kHz / 12.48 s Korean speech.
Run actual local whisper-small.
Review transcript quality against prior reference evidence.
Preview/save/export required.

## 9. Product E2E
All four lanes must be executed through the approved product UI/runtime with actual local outputs.
Remote SUCCESS remains a baseline, not a substitute for local execution.

## 10. Quality + regression
Quality-review all four lanes.
Run Python/UI/server/evidence/package regressions.
UI_SSOT_CHANGED:NO.

## 11. Evidence
Create review + JSON with:
three local artifact paths/digests; package extraction inventory; model/runtime identities; adapter fixes; actual input/output hashes; PHOTO/UPSCALE before-after quality; VIDEO continuity; STT transcript notes; Preview/save/export; regression; commit/PR/Actions.

## 12. Verdict
Remote package creation is PASS and frozen.
Only local runtime/quality lanes remain.
Do not repeat remote acquisition.
PC_LOCAL_FINAL_E2E_PASS only when all local lanes + Preview/save/export + regression pass.

## 13. Automatic next directive
Immediately after review create/commit next directive. Do not wait for user.

## Protection
No UI redesign, main merge, Production deploy, force push, GPU, paid compute, credential exposure, legacy fallback, or prior Evidence overwrite.
